export const foundationsKnowledge = `
# Foundations: Points, Lines, Rays, Planes & Angles

## The Basic Building Blocks

A **point** marks an exact location. It has no size at all — no width, no length, no thickness. We label points with capital letters (point A, point B).

A **line** is perfectly straight and goes on forever in both directions. It has no endpoints. We show this with arrows on both ends. Two points determine exactly one line.

A **line segment** is the piece of a line between two endpoints. Unlike a line, it has a measurable length.

A **ray** starts at one endpoint and goes on forever in one direction — like a beam of light from a torch.

A **plane** is a perfectly flat surface that extends forever in all directions — like an endless tabletop. It has length and width but no thickness.

## Lines and Their Relationships

**Parallel lines** never meet. They stay the same distance apart no matter how far they go. Think of train tracks.

**Perpendicular lines** meet at exactly 90° — a right angle. The corner of a piece of paper is a good example.

**Intersecting lines** cross at exactly one point. Two straight lines can only ever meet at one point.

When a line crosses two parallel lines, it creates pairs of equal angles:
- **Corresponding angles** are in matching positions — they are always equal.
- **Alternate interior angles** are on opposite sides of the crossing line, between the parallel lines — they are always equal.

## Angles

An **angle** is formed when two rays share the same endpoint (called the vertex).

**Types of angles:**
- **Acute**: less than 90° — a sharp, pointy corner
- **Right**: exactly 90° — marked with a small square symbol
- **Obtuse**: between 90° and 180° — wider than a right angle, like an open book
- **Straight**: exactly 180° — looks like a flat line
- **Reflex**: between 180° and 360° — the "long way around"

**Angle pairs:**
- **Complementary angles** add up to 90°. If one is 30°, the other is 60°.
- **Supplementary angles** add up to 180°. If one is 110°, the other is 70°.
- **Vertical angles** are across from each other when two lines cross. They are always equal.
- **A linear pair** sits on a straight line. The two angles always add up to 180°.

**A full rotation** around a point is 360°.

## Triangles and Angles

The three angles inside any triangle always add up to exactly 180°. Always — no exceptions.

This means:
- You can always find the third angle if you know the other two.
- A triangle can never have two obtuse angles (they'd already exceed 180°).
- A right triangle's two non-right angles always add up to 90°.
`;
