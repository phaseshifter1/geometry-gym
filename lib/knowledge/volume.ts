export const volumeKnowledge = `
# Heavy Lifts: Volume & Surface Area

## The Key Difference

**Volume** = the amount of space inside a 3D shape — how much it can hold. Measured in cubic units: cm³, m³, in³.

**Surface area** = the total area of all the faces (surfaces) of a 3D shape — how much material you'd need to wrap the outside. Measured in square units: cm², m².

Think of a cardboard box: the surface area is all the cardboard used to make it, the volume is how much fits inside it.

## Key 3D Shapes

**Rectangular prism (cuboid)**: A box shape — all faces are rectangles.
- Volume: V = length × width × height
- Surface area: SA = 2(lw + lh + wh) — two of each pair of opposite faces

**Cube**: A special rectangular prism where all sides are equal (l = w = h = s).
- Volume: V = s³
- Surface area: SA = 6s² — six identical square faces

**Cylinder**: A circular tube with flat circular ends.
- Volume: V = πr²h (base area × height)
- Surface area: SA = 2πr² + 2πrh (two circles + the curved side)

**Pyramid**: A base shape with triangular faces meeting at a point (apex).
- Volume: V = ⅓ × base area × height
- A pyramid holds exactly one-third the volume of a prism with the same base and height.

**Cone**: A circular base tapering to a point.
- Volume: V = ⅓ × πr²h

**Sphere**: A perfectly round 3D shape.
- Volume: V = (4/3)πr³

## Faces, Edges, and Vertices

Every 3D shape can be described by:
- **Faces**: the flat surfaces
- **Edges**: where two faces meet
- **Vertices**: the corner points where edges meet

A cube: 6 faces, 12 edges, 8 vertices.

Euler's formula connects them: Faces + Vertices − Edges = 2. (Works for any convex polyhedron.)

## Nets

A **net** is what a 3D shape looks like when unfolded flat. It shows all the faces laid out. Folding a net along the edges recreates the 3D shape.

Nets are useful for calculating surface area — add up the area of each face in the net.

## Units

Volume uses cubic units because you multiply three lengths: l × w × h.
Surface area uses square units because you're measuring area (flat faces).

cm × cm × cm = cm³ ✓
cm × cm = cm² ✓
`;
